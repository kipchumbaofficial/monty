#include "monty.h"
/**
 * main - Entry point
 * @argv: Argument vector
 * @argc: Argument count
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	if (argc == 2)
	{
		filehandle(argv[i]);
	}
	else
	{
		dprintf(STDERR_FILENO, "USAGE: Montyfile\n");
		exit(EXIT_FAILURE);
	}
	return (0);
}
